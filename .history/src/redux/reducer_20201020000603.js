const INITIAL_STATE = {
    term:null,
};

const actionTypes = {
    SET_SEARCH_TERMS: "SET_SEARCH_TERMS",
};

const reducer = (state, action) => {
    switch (action.type) {
        case actionTypes.SET_SEARCH_TERMS:
            return 
    }
}